@{
    Layout = "_Layout";
}
@using System.Data;
@model DataTable

<div class="card">
    <div class="card-header d-flex justify-content-between">
        <h3>Bus List</h3>
        <div>
            <a type="button" class="btn btn-success btn-add">Add New Bus</a>
        </div>
    </div>
    <div class="card-body">

        <div class="table-responsive">

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">Bus Number</th>
                        <th scope="col">Bus Type</th>
                        <th scope="col">Manufacturer</th>
                        <th scope="col">ModelNo</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody id="myTable">

                    @if (Model.Rows.Count <= 0)
                    {
                        <tr>
                            <td class="col text-danger text-center">Norecord</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (DataRow dataRow in Model.Rows)
                        {
                            <tr>
                                <td>@dataRow["RegistrationNumber"]</td>
                                <td>@dataRow["TypeName"]</td>
                                <td>@dataRow["Manufacturer"]</td>
                                <td>@dataRow["ModelNo"]</td>


                                <td>
                                    <a type="button" class="btn btn-danger">Delete</a>
                                    <a type="button" class="btn btn-info">edit</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>
